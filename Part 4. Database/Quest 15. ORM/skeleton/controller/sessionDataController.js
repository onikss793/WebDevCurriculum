const noteDao = require('../dao/noteDao');

const uploadSessionData = (req, res, next) => {
    const data = req.body;
};

const loadSessionData = (req, res, next) => {};

module.exports = { uploadSessionData, loadSessionData };
